{
  "homepage": "https://sayanpark.github.io/DjangonReact/",
  "scripts": {
    "build": "cd frontend && npm run build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d frontend/dist",
    "start-backend-tunnel": "concurrently \"lt --port 8000 --subdomain backend-szkblog\" \"python backend/manage.py runserver 0.0.0.0:8000\""
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "7.0.0",
    "bootstrap": "^5.3.6",
    "express": "^5.1.0",
    "gh-pages": "^6.3.0",
    "http-proxy-middleware": "^3.0.5"
  },
  "devDependencies": {
    "localtunnel": "2.0.2",
    "concurrently": "9.2.0"
  }
}
