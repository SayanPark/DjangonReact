services:
  - type: web
    name: backend
    env: python
    rootDirectory: backend
    buildCommand: python -m pip install -r requirements.txt
    preStartCommand: python -m pip show gunicorn
    startCommand: python -m gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT
    autoDeploy: true
    plan: free
